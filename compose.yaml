services:
  web:
    build: .
    container_name: django_dev

    command: sh -c "python manage.py migrate && python manage.py populate && python manage.py runserver 0.0.0.0:8000"

    ports:
      - '8000:8000'

    volumes:
      - .:/app:Z

    stdin_open: true
    tty: true
